# readonly_output：终端只读结果接口

这是服务器2对终端开放的唯一结果输出模块。只允许读取审核通过、与当前用户和项目绑定的结果。

必须支持：短时/一次性授权令牌、结果ID绑定、用户和项目权限校验、结果状态校验、下载范围限制、审计记录和断点下载。

禁止：上传、目录列表、路径下载、原始数据访问、VeraCrypt盘符访问、任意文件读取和管理接口。

终端到服务器2只开放此模块的业务端口。